language: node_js
node_js:
  - "node"
before_script:
  - npm install -g gulp
  - npm install -g ci-publish
  - npm install -g gulp-codecov
  - npm install
after_success:
  - bash <(curl -s https://codecov.io/bash)
script:
  - gulp
